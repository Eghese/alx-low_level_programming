#include <stdio.h>
/**
 * Description: main - printing the output of an alphabet.
 *
 * putchar: return an output
 */
int main()
{
	int ch;

	for(ch = 'a'; ch <= 'z'; ch++)
	{
		putchar(ch);
	}
	return (0);
}
