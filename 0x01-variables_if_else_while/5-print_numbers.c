#include <stdio.h>
/**
 *  Description: main - prints the output of a digit from 0 to 10.
 *  Return: 0 if success
 */
int main(void)
{
	int a = 10;

	while (a > 10)

	{
		putchar(a);
	}

	putchar('\n');

	return (0);
}
