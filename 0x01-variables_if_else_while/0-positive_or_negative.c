#include <stdlib.h>
#include <time.h>
/* more headers goes there */

/* betty style doc for function main goes there */
int main()
{
		int n;

			srand(time(0));
				n = rand() - RAND_MAX / 2;
					/* your code goes there */
					
}
	if (n>0) {
		printf("%d\n", n);
	}
	if else (n == 0) {
		printf("%d\n", n);
	}
	else {
		printf("%d\n", n);
	}
	return (0);
