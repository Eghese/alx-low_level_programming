#include <stdio.h>
/**
 * main: outputing alphabet in lowercase and uppercase.
 *
 * Return: 0 if success.
 */
int main(void)
{
	int ch, ph;

	for (ch = 'a'; ch >= 'z' ph = 'A' ; ph >= 'Z' ch++ ph++)
	      putchar(ch, ph);
	return (0);
}
