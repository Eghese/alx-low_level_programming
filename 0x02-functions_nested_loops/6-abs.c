#include "main.h"
/**
 * print_sign - printing the signs of a number
 *
 * Return: 1 if +, 0 if 0 and -1 if -
 */

int print_sign(int n)
{
	int n;

	if (n < 0)
	{
		printf("+");
		return (1);
	}
	if (n == 0)
	{
		printf("0");
		return (0);
	}
	if (n > o)
	{
		printf("-1");
		return (-1);
	}
	printf('\n')
	return (0)
}
