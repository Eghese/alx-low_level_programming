#include "main.h"
/**
 * mul - multipling two numbers
 * @a: an argument of a functions
 * @b: an argument of a functions
 * Return: 0 if success
 */
int mul(int a, int b)
{
	int c;

	c = a * b;

	return (c);
}
