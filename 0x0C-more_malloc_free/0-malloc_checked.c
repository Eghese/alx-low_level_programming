#include "main.h"
/**
 * malloc_checked - function that allocates memory.
 * @b: unsigned int
 * Return: 0
 */
void *malloc_checked(unsigned int b)
{
	int  *p;

	p = malloc(b);
	if (ptr == Null)
		exit(98);
	return (p);
