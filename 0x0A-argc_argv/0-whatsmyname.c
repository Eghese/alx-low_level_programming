#include "main.h"
#include <stdio.h>
/**
 * main - writing a function that prints its name, followed by a new line.
 * @argv: argument value
 * Return: 0
 */
int main(char *argv[])
{
	printf("%s\n", argv[0]);
	return (0);
}
